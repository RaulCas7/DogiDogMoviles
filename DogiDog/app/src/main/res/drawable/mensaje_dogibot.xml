<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white"/> <!-- Fondo del mensaje del bot -->
    <corners android:radius="15dp"/> <!-- Bordes redondeados -->
    <stroke
        android:color="@color/primario"
    android:width="2dp"/>
</shape>